<h1>ini adalah halaman contact</h1>

<section>
    <h1>ini adalah data mahasiswa</h1>

    <a href="/contact/tambah" class="btn btn-success my-3">Tambah Mahasiswa</a>





    <% if (mahasiswa.length === 0) { %>
        <div class="alert alert-primary" role="alert">
            maaf data mahasiswa kosong
          </div>
    <% }else{ %>
        <table class="table">
            <thead>
              <tr>
                <th scope="col">No</th>
                <th scope="col">Nama</th>
                <th scope="col">Email</th>
                <th scope="col">Detil</th>
    
              </tr>
            </thead>
            <tbody>
    
                <% mahasiswa.forEach((mhs,i) => { i++%>
                    <tr>
                        <th scope="row"><%= i %> </th>
                        <td><%= mhs.nama %> </td>
                        <td><%= mhs.email %> </td>
                        <td><a href="/contact/<%= mhs.nama %> " class="btn btn-success">detil</a></td>
                      </tr>
                <% }) %>
    
            </tbody>
          </table>
    <% } %> 
</section>